name: Build Android Binaries (Static)

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag (e.g. 2026.06.13)"
        required: false
        default: ""

permissions:
  contents: write

jobs:
  # ─── arm64-v8a ────────────────────────────────────────────────────────────────
  build-arm64:
    name: Android arm64-v8a
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v4

      - name: Build + staticx inside musllinux Docker (aarch64)
        working-directory: zeusdl/zeusdl
        run: |
          chmod +x bundle/docker/linux/build.sh

          cat > /tmp/build_and_staticx.sh << 'INNEREOF'
          #!/bin/bash
          set -ex

          # 1. Build the PyInstaller binary
          bash /yt-dlp/bundle/docker/linux/build.sh

          # 2. Export PYTHONPATH so all subsequent commands in this shell see pip
          PY_PURELIB="$(py3.13 -c 'import sysconfig; print(sysconfig.get_path("purelib"))')"
          export PYTHONPATH="$PY_PURELIB"

          # 3. Create a venv without pip bootstrap
          py3.13 -m venv /tmp/sx_venv --without-pip

          # 4. Install setuptools + SCons into the venv via "python -m pip"
          #    (PYTHONPATH provides pip; installs to venv site-packages)
          /tmp/sx_venv/bin/python -m pip install setuptools SCons --quiet

          # 5. Add venv/bin to PATH so scons entry-point is found by staticx build
          export PATH="/tmp/sx_venv/bin:$PATH"

          # 6. Install staticx + patchelf; --no-build-isolation uses setuptools/SCons
          #    already in the venv. pip creates /tmp/sx_venv/bin/staticx script.
          /tmp/sx_venv/bin/python -m pip install staticx patchelf --no-build-isolation --quiet

          # 7. Embed musl linker → fully static binary
          EXE="/yt-dlp/dist/${EXE_NAME}"
          /tmp/sx_venv/bin/staticx "${EXE}" "${EXE}-static"
          mv "${EXE}-static" "${EXE}"

          file "${EXE}"
          ls -lh "${EXE}"
          INNEREOF

          chmod +x /tmp/build_and_staticx.sh

          docker run --rm \
            --workdir /yt-dlp \
            -v "$(pwd):/yt-dlp" \
            -v "/tmp/build_and_staticx.sh:/build_and_staticx.sh:ro" \
            -e EXE_NAME="zeusdl-android-arm64" \
            -e CHANNEL="stable" \
            -e ORIGIN="ferelking242/zeusdl" \
            -e VERSION="${{ inputs.tag || github.run_number }}" \
            -e REQUIREMENTS="musllinux-aarch64" \
            -e SKIP_ONEDIR_BUILD="1" \
            ghcr.io/yt-dlp/musllinux_1_2_aarch64-shared:latest \
            bash /build_and_staticx.sh

          # Docker runs as root → files are root:root 700. Fix so runner can read them.
          sudo chmod -R a+r dist/

      - name: Verify — statically linked
        working-directory: zeusdl/zeusdl
        run: |
          file dist/zeusdl-android-arm64 | grep -q 'statically linked' || \
            (echo "ERROR: binary is not statically linked" && exit 1)
          echo "OK — arm64 binary is fully static"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: zeusdl-android-arm64
          path: zeusdl/zeusdl/dist/zeusdl-android-arm64

  # ─── x86_64 ───────────────────────────────────────────────────────────────────
  build-x86_64:
    name: Android x86_64
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - name: Build + staticx inside musllinux Docker (x86_64)
        working-directory: zeusdl/zeusdl
        run: |
          chmod +x bundle/docker/linux/build.sh

          cat > /tmp/build_and_staticx.sh << 'INNEREOF'
          #!/bin/bash
          set -ex

          bash /yt-dlp/bundle/docker/linux/build.sh

          PY_PURELIB="$(py3.13 -c 'import sysconfig; print(sysconfig.get_path("purelib"))')"
          export PYTHONPATH="$PY_PURELIB"

          py3.13 -m venv /tmp/sx_venv --without-pip

          /tmp/sx_venv/bin/python -m pip install setuptools SCons --quiet

          export PATH="/tmp/sx_venv/bin:$PATH"

          /tmp/sx_venv/bin/python -m pip install staticx patchelf --no-build-isolation --quiet

          EXE="/yt-dlp/dist/${EXE_NAME}"
          /tmp/sx_venv/bin/staticx "${EXE}" "${EXE}-static"
          mv "${EXE}-static" "${EXE}"

          file "${EXE}"
          ls -lh "${EXE}"
          INNEREOF

          chmod +x /tmp/build_and_staticx.sh

          docker run --rm \
            --workdir /yt-dlp \
            -v "$(pwd):/yt-dlp" \
            -v "/tmp/build_and_staticx.sh:/build_and_staticx.sh:ro" \
            -e EXE_NAME="zeusdl-android-x86_64" \
            -e CHANNEL="stable" \
            -e ORIGIN="ferelking242/zeusdl" \
            -e VERSION="${{ inputs.tag || github.run_number }}" \
            -e REQUIREMENTS="musllinux-x86_64" \
            -e SKIP_ONEDIR_BUILD="1" \
            ghcr.io/yt-dlp/musllinux_1_2_x86_64-shared:latest \
            bash /build_and_staticx.sh

          # Fix Docker root-owned file permissions
          sudo chmod -R a+r dist/

      - name: Verify — statically linked
        working-directory: zeusdl/zeusdl
        run: |
          file dist/zeusdl-android-x86_64 | grep -q 'statically linked' || \
            (echo "ERROR: binary is not statically linked" && exit 1)
          echo "OK — x86_64 binary is fully static"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: zeusdl-android-x86_64
          path: zeusdl/zeusdl/dist/zeusdl-android-x86_64

  # ─── Publish Release ──────────────────────────────────────────────────────────
  release:
    name: Publish Release
    needs: [build-arm64, build-x86_64]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: Compute SHA-256 checksums
        run: |
          cd dist
          sha256sum zeusdl-android-arm64 zeusdl-android-x86_64 | tee SHA256SUMS.txt

      - name: Compute tag name
        id: tag
        run: |
          TAG="${{ inputs.tag }}"
          if [[ -z "$TAG" ]]; then
            TAG="$(date -u '+%Y.%m.%d')"
          fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          name: "ZeusDL Android — ${{ steps.tag.outputs.tag }}"
          prerelease: false
          body: |
            ## ZeusDL Android — Binaires 100% Statiques

            Compilés avec **musl Python 3.13 + PyInstaller + staticx**.
            Le linker musl est **embarqué dans le binaire** — aucune dépendance externe.
            Compatible Android 5.0+ (API 21+), arm64-v8a et x86_64.

            | Fichier | Architecture |
            |---------|-------------|
            | `zeusdl-android-arm64` | arm64-v8a (quasi tous les appareils modernes) |
            | `zeusdl-android-x86_64` | x86_64 (émulateurs, Chromebooks x86) |
          files: |
            dist/zeusdl-android-arm64
            dist/zeusdl-android-x86_64
            dist/SHA256SUMS.txt
